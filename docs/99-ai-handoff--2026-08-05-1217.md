# triageRush AI Handoff: Phase 7 Complete, Phase 8 Next

**Last modified:** 2026-08-05

**Latest change:** Phase 7 built and machine-verified (scheduler, countdown,
clock sounds, RUSH numerals, RUSH arrivals/bursts) plus the session's clock
and clipboard-header decisions. Awaiting John's play-through verdict.

**Current version:** 2026-08-05 12:17 PDT

## Start here next session

1. Documents `1` through `9` are current: they describe the game as designed
   AND as built through Phase 7. There is no amendment layer.
2. The working game is repo-root `index.html` plus `triageRush/{assets.js,
   game.js, ui.js, app.js, styles.css}`. John runs it with
   `start-triageRush.bat` (port 8090). Never open `index.html` from `file://`
   (fetch fails). AI test servers use any other port and are killed after use.
3. Cache busting: one version string in TWO places — `index.html` query
   params (5 refs) and `TRIAGE_RUSH_CACHE_VERSION` in `assets.js`. Currently
   `2026-0805-phase7i`; bump both on every asset/CSS/JS change.
4. Phase 7 landed 2026-08-05 but John has not yet given his play-through
   verdict — expect sound/visual tuning tweaks (tick pitches/volumes, numeral
   feel, chart-header spacing are all one-number changes).
5. The next implementation work is **Phase 8** of document `9`: Shift Review
   and Patients Seen (the chart builder's `"review"` setting). But see the
   backlog below — John may want small items first.

## Phase 7 decisions that changed earlier plans (all in docs now)

- The Chart does NOT pause the clock (reading a patient costs shift time).
  Pause reasons are only `confirmation` and `document-hidden`. This means
  RUSH arrivals also continue while the chart is open.
- The clock starts after a 2-second acclimation delay once GAME appears;
  RUSH's start tick plays when the clock starts, not on screen entry.
- The final-countdown cadence: whole-second countdown ticks 10..1, extras at
  N-0.25/N-0.50 during the final five, and the last TWO seconds beat on
  every quarter as a run-in to the endDong.
- RUSH numerals pop centered on the middle of the patient image and expand/
  dissolve (scale 1.7, fade, slight blur) over ~500ms. RUSH-only; Triage
  gets the audio cadence but no numerals.
- The clipboard's metal clamp was replaced by a header row: PATIENT CHART
  left-justified, the LIVE shift clock (mirrors the header timer every
  second), then the red X. Retired clamp CSS is kept commented in styles.css.
- The staged burst second-arrival timestamp is LOGICAL game time
  (`state.shift.elapsedMs` based), so pauses freeze it.

## Build state (verified 2026-08-05)

- Phases 1-6 implemented and visually approved by John (desktop + iPhone).
- Phase 7 implemented and machine-verified: three Node harnesses (scheduler
  quarters/pauses/timer-end; full-shift cue schedules for RUSH 60/120 and
  Triage 300/600; arrivals/bursts against doc 9's tables) all pass, plus
  chrome-devtools live runs (countdown, pause behavior, numerals 10..1,
  queue growth 2→10 on the shrinking schedule, full-queue shake, timer-zero
  into review). Harnesses are ephemeral (session scratchpad) — rebuild from
  doc 9's acceptance tables.
- Only known console noise: a harmless favicon 404.

## Standing constraints (do not relearn these the hard way)

- One small change at a time; John reviews visually in his own browser after
  each change and gives the verdict before the next step.
- The approved patient-panel layout is final; fix text overflow with auto-fit
  shrinking (`fitTextToBox` is the sanctioned future approach), never by
  reworking the layout.
- Invariant warnings from `assertStateInvariants` mean a transition bug;
  never patch state directly.
- Triangle/arrow glyphs need U+FE0E variation selectors or iPhones render
  boxed emoji.
- `game.js` runs under Node with a tiny shim (global window +
  TRIAGE_RUSH_ASSETS stub + performance) for deterministic tests.

## Backlog (John's items first)

- **Confirm-dialog button redesign (John, 2026-08-05)**: in both the QUIT
  THIS SHIFT and END SHIFT EARLY confirmation dialogs, "Whoops! I want to
  keep playing!" moves to centered under the headline text with a green box
  background. Below it sits the destructive choice in red — "Yes, quit this
  shift" with a larger LEFT-pointing arrow, "Yes, end shift early" with a
  larger RIGHT-pointing arrow. (Use text arrows with U+FE0E so iPhones do
  not render boxed emoji.)

- **Shift-end intermediate screen (John, 2026-08-05, consider later)**: when
  the shift ends, instead of jumping straight into SHIFT REVIEW, consider a
  transition screen saying something like "Shift Over..." with two options:
  `◀︎━━ RETURN TO ER ENTRANCE` and `REVIEW SHIFT ━━▶︎` (text arrows with
  U+FE0E, matching the footer's left/right arrow language). Would apply to
  the timer-zero ending and possibly END SHIFT EARLY; note doc 9 currently
  says review opens immediately and review's only exit is Return to ER
  Entrance, so this changes that flow if adopted.

- **Countdown numerals over the Chart / photo zoom (John, 2026-08-05,
  consider later - NO decision yet)**: the RUSH final-countdown numerals
  currently pop only over the patient image in the center panel, which the
  Chart overlay (and its photo-zoom lightbox) covers. Consider also showing
  them on top of the open patient chart and/or the magnified patient photo
  so the final ten stays visible while reading. The clock already runs and
  the countdown ticks are already audible under the Chart, so this is
  purely a visual-layer question (where a second `.countdown-numeral`
  mount would live and at what z-index).

- **Scorecard hover magnification (John, 2026-08-05, consider later)**: when
  the player hovers the scorecard on the game screen, a magnified version
  appears showing a more detailed breakdown (Correct: 5, Close: 2, Wrong: 3).
  Note the score selectors already expose these counts
  (`selectScoreTotals`); this is presentation work only. Needs a
  touch-device answer too, since iPhones have no hover.

- Phase 8: Shift Review + Patients Seen (chart builder's `"review"` setting).
- Phase 9: persistence and recovery.
- Phase 10 planning must ALSO rework the asset-loading strategy: stage-2
  startup currently blocks READY on 160 patient-JSON fetches plus decoding
  every manifest image (~187 MiB). Decide what blocks READY vs lazy-loads.
- Layered room rendering (wall → interior → patient → door) — a visual
  interlude John wants around Phase 7-8; he will say when.
- Six vital icons still need to be generated (`assets/icons/` is empty).
- Settings/About popup text is placeholder; fix before release.
- Archiving unused art (boombox, panel bubbles) awaits John's OK.
- Add a favicon someday.
