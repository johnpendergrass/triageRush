# Artwork Development

- `iterations/` preserves generated experiments, mockups, source prompts, and
  associated specifications.
- `selected-reference/` preserves the former selected-artwork snapshot for
  evaluation during production development.

Neither folder is loaded by the production app. Optimized artwork approved for
runtime use belongs under `../../../assets/`.
